# -*- mode: python ; coding: utf-8 -*-
# Dataset capture tool: only camera profile is shipped, never config/vlm.yaml.

a = Analysis(
    ['scripts\\dataset_capture.py'],
    pathex=['src'],
    binaries=[],
    datas=[('config\\camera_profile.yaml', 'config')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='DatasetCapture',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='DatasetCapture',
)
